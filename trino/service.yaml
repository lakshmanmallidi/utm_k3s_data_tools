apiVersion: v1
kind: Service
metadata:
  name: trino
  labels:
    app: trino
spec:
  type: LoadBalancer
  selector:
    app: trino
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30402
