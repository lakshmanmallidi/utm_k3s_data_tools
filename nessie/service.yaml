apiVersion: v1
kind: Service
metadata:
  name: nessie
  labels:
    app: nessie
spec:
  type: ClusterIP
  selector:
    app: nessie
  ports:
    - name: http
      port: 19120
      targetPort: 19120
